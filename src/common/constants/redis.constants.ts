export const REDIS_CLIENT = Symbol("REDIS_CLIENT")

export const REDLOCK_CLIENT = Symbol("REDLOCK_CLIENT")

export const CACHE_TTL = {
  /** Time-to-live for authentication codes in minutes */
  AUTH_CODE_MINUTES: 5,
}
